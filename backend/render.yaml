services:
  - type: web
    name: appro-marketplace-api
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    startCommand: node src/server.js
    healthCheckPath: /health
    envVars:
      - key: JWT_SECRET
        generateValue: true
