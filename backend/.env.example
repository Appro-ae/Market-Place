# Copy to .env for local dev. On Render/Railway, set these as environment variables.
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
# Optional: absolute path for the SQLite file (defaults to backend/data.sqlite)
# DATABASE_PATH=/data/data.sqlite
